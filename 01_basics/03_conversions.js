let value=54

console.log(typeof value)

let isLoggedIn="Hiten"

let BooleanIsLoggedIn= Boolean(isLoggedIn)
console.log(BooleanIsLoggedIn)

// If we convert 1 into boolean output is true
//Similarly if we convert 0 into boolean output is false

// If we convert "" into boolean output is true
//if "Value" output is false